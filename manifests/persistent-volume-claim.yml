apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: text-generation-webui-pvc
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi